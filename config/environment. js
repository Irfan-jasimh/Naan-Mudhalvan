const environment = {
    development: {
        isProduction: false,
        port: process.env.PORT || 3000
    },
    production: {
        isProduction: true,
        port: process.env.PORT || 3000
    }
};

module.exports = environment[process.env.NODE_ENV || 'development'];
